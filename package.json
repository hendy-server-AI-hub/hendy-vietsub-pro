{
  "name": "hendy-vietsub-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
